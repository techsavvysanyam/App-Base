<resources>
    <style name="Theme.AppBase" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary Colors -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <!-- Secondary Colors -->
        <item name="colorSecondary">@color/accent_color</item>
        <item name="colorSecondaryVariant">@color/primary_dark</item>
        <item name="colorOnSecondary">@color/white</item>
        
        <!-- Surface Colors -->
        <item name="colorSurface">@color/surface_primary_dark</item>
        <item name="colorOnSurface">@color/text_primary_dark</item>
        <item name="colorOnSurfaceVariant">@color/text_secondary_dark</item>
        
        <!-- Background Colors -->
        <item name="android:colorBackground">@color/background_primary_dark</item>
        <item name="colorOnBackground">@color/text_primary_dark</item>
        
        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Other Colors -->
        <item name="colorOutline">@color/surface_tertiary_dark</item>
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/white</item>
        
        <!-- Typography -->
        
        <!-- Card Style -->
        <item name="materialCardViewStyle">@style/Widget.AppBase.CardView.Dark</item>
        <item name="buttonStyle">@style/Widget.AppBase.Button</item>
    </style>

    <!-- Custom Card Style -->
    <style name="Widget.AppBase.CardView.Dark" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">4dp</item>
        <item name="cardBackgroundColor">@color/card_background_dark</item>
        <item name="android:layout_margin">8dp</item>
    </style>
</resources>
